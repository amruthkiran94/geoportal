# MinIO credentials — copy to .env and fill in values
# .env is gitignored and must never be committed

MINIO_ROOT_USER=your_access_key_here
# Minimum 8 characters
MINIO_ROOT_PASSWORD=your_secret_key_here
